{
  "name": "mono-zod",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "clean": "find . -type d -name node_modules -o -name dist -o -name .venv -o -name .next -o -name .turbo | xargs rm -rf"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "prettier": "^2.6.1",
    "tsup": "^5.12.5",
    "turbo": "^1.2.1",
    "typescript": "^4.6.3"
  }
}
